# Marooff admin CMS — PRODUCTION env (used at BUILD time).
#
# Rename to `.env.local` before running `npm run build`.
# Baked into the SPA bundle at build time.

VITE_API_URL=https://api.marooffc.com/api
